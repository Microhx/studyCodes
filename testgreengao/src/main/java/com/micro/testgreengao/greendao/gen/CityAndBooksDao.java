package com.micro.testgreengao.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.micro.testgreengao.bean.one2many3.CityAndBooks;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CITY_AND_BOOKS".
*/
public class CityAndBooksDao extends AbstractDao<CityAndBooks, Long> {

    public static final String TABLENAME = "CITY_AND_BOOKS";

    /**
     * Properties of entity CityAndBooks.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property _id = new Property(0, Long.class, "_id", true, "_id");
        public final static Property BookId = new Property(1, long.class, "bookId", false, "BOOK_ID");
        public final static Property CityId = new Property(2, long.class, "cityId", false, "CITY_ID");
    }


    public CityAndBooksDao(DaoConfig config) {
        super(config);
    }
    
    public CityAndBooksDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CITY_AND_BOOKS\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: _id
                "\"BOOK_ID\" INTEGER NOT NULL ," + // 1: bookId
                "\"CITY_ID\" INTEGER NOT NULL );"); // 2: cityId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CITY_AND_BOOKS\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, CityAndBooks entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
        stmt.bindLong(2, entity.getBookId());
        stmt.bindLong(3, entity.getCityId());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, CityAndBooks entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
        stmt.bindLong(2, entity.getBookId());
        stmt.bindLong(3, entity.getCityId());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public CityAndBooks readEntity(Cursor cursor, int offset) {
        CityAndBooks entity = new CityAndBooks( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // _id
            cursor.getLong(offset + 1), // bookId
            cursor.getLong(offset + 2) // cityId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, CityAndBooks entity, int offset) {
        entity.set_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setBookId(cursor.getLong(offset + 1));
        entity.setCityId(cursor.getLong(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(CityAndBooks entity, long rowId) {
        entity.set_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(CityAndBooks entity) {
        if(entity != null) {
            return entity.get_id();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(CityAndBooks entity) {
        return entity.get_id() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
